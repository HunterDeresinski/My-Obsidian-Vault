import random
from opt2 import opt2_successor

def first_choice_hillclimbing(obj, points,
                              successor_method='p2',
                              maxtries=None):
    """
    Termination condition: max number of tries if len(points) by default
    """
    
    print("*** NOT IMPLEMENTED ***")
    
